## 2025-10-03 - Parameter Optimization Script Fixes

This update addresses several issues encountered while setting up and running the parameter optimization script.

### Bug Fixes and Improvements

*   **Model Registry Consistency:** Corrected inconsistencies in model naming between `config_optimizer.py`, `registry.py`, and `optimize_parameters.py`.
    *   Updated `src/optitrade/config_optimizer.py` to use `PriceTrendModel` as key instead of `price_trend`.
    *   Updated `src/optitrade/models/registry.py` to use model class names (e.g., `PriceTrendModel`) as keys in `MODEL_REGISTRY` for consistency.
*   **Model Initialization:** Ensured `scripts/optimize_parameters.py` correctly calls `initialize_models()` to populate the model registry before attempting to retrieve models.
*   **Backtester Class Name:** Corrected the import and usage of the `Backtester` class in `scripts/optimize_parameters.py` from the incorrect `BacktestSimulator` to the proper `Backtester` name.